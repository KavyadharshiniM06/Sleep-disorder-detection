# Sleep-disorder-detection
Detecting sleep disorders using machine learning on health and lifestyle data. Includes data preprocessing, model training with Random Forest, hyperparameter tuning via GridSearchCV, and visualizations like feature importance, confusion matrix, and correlation heatmap.
